{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-end p-3">
        <button class="btn bouton-formulaire" id="bouton_envoi">Enregistrer l'adresse de livraison</button>
    </div>
{{ form_end(form) }}