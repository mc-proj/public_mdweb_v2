{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-end p-3">
        <button class="btn bouton-formulaire" id="bouton-message">Enregistrer le message</button>
    </div>
{{ form_end(form) }}